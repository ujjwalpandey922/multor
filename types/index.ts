export interface cardProps {
  rating: number;
  name: string;
  location: string;
  message: string;
  url: string;
}

export interface featureProps {
  name: string;
  message: string;
  url: string;
}